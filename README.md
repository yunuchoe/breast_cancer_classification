# Breast Cancer Classification 

This is an ECE 403 (ML Optimization) lab work implementing a logistic regression classifier in MATLAB. 

Instead of utilizing pre-built machine learning libraries, this project evaluates the Wisconsin Diagnostic Breast Cancer (WDBC) dataset by performing manual feature normalization and utilizing a custom gradient descent algorithm to optimize model weights. The final generalization performance is scored via a confusion matrix and accuract score.
